package com.dbright.Test;

public class mogon {
/*    query.addCriteria(Criteria.where("user_id").in(userIds));
            query.addCriteria(Criteria.where("timestamp").gte(startTimeParse / 1000).lte(endTimeParse / 1000));
    //            query.addCriteria(Criteria.where("client_typchae").ne("web"));
    Criteria criteria1 = Criteria.where("client_type").is("ios");
    Criteria criteria2 = Criteria.where("client_type").is("android");
            query.addCriteria(new Criteria().orOperator(criteria1,criteria2));*/
}
